# Operator index release history

This directory contains records of past ACS operator-index releases.
Each file contains `Snapshot` and `Release` resources for all OCP versions updated in a given operator index release.
